{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "postinstall": "nuxi prepare",
    "deploy": "cp CNAME .output/public && gh-pages --dotfiles -d .output/public"
  },
  "devDependencies": {
    "gh-pages": "^6.1",
    "nuxt": "^3.11",
    "nuxt-gtag": "^3.0.2",
    "sass": "^1.72"
  }
}
