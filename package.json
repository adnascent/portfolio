{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxi clean && nuxi dev",
    "build": "nuxi build",
    "start": "nuxi start",
    "generate": "nuxi generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^3.3.2",
    "@nuxt/vite-builder": "^3.3.2"
  },
  "devDependencies": {
    "sass": "^1.60.0",
    "push-dir": "^0.4.1"
  }
}
